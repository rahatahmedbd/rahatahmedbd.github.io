# Source assets

This directory is reserved for build-time assets that should be imported by application code in later phases.

The current portfolio's existing public images live in `public/assets/` so their original URLs remain stable. Do not move them without an intentional asset migration and redirect plan.
